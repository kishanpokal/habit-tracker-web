"use client";

import RitualisLogo from "./RitualisLogo";

/**
 * HabitFlowLogo is now an alias for RitualisLogo
 * (Preserved for backwards compatibility while rebranding to Ritualis)
 */
export default RitualisLogo;
export { RitualisLogo };
